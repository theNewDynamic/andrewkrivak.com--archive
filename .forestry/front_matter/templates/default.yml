---
label: default
hide_body: false
fields:
- type: include
  name: defaults
  label: defaults
  template: partial-default
- type: include
  name: weight
  label: weight
  template: partial-weight
- name: image
  type: file
  config:
    maxSize: 250
  label: Image
